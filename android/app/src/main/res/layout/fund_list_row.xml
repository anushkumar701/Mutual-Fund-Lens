<?xml version="1.0" encoding="utf-8"?>
<!-- Single row in the per-fund widget ListView -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="4dp"
    android:background="#0D1B2E">

    <!-- Fund name -->
    <TextView
        android:id="@+id/row_fund_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Fund Name"
        android:textColor="#e2e8f0"
        android:textSize="10sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginBottom="2dp" />

    <!-- Invested / Current / Gain row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/row_invested"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Inv: ₹0"
            android:textColor="#64748b"
            android:textSize="9sp" />

        <TextView
            android:id="@+id/row_current"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Now: ₹0"
            android:textColor="#cbd5e1"
            android:textSize="9sp" />

        <TextView
            android:id="@+id/row_gain"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="+₹0 (0%)"
            android:textColor="#10b981"
            android:textSize="9sp"
            android:gravity="end" />
    </LinearLayout>

    <!-- Thin divider between rows -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#1e293b"
        android:layout_marginTop="4dp" />
</LinearLayout>
